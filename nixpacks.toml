[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "python3 -m venv /app/.venv",
  "/app/.venv/bin/pip install -r backend/requirements.txt"
]

[start]
cmd = "/app/.venv/bin/uvicorn backend.server:app --host 0.0.0.0 --port ${PORT:-8000}"
